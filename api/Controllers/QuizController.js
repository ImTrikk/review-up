export const QuizController = async (req, res) => {
	// get the QuizData
	// get the user_id
	// get quiz_id
	try {
		// query the course using quiz_id
		// hanlde query query quiz data to quiz table
	} catch (err) {
		return res.status(500).json({ message: "Internal server error" });
	}
};
